pipeline {
    agent any
    tools {
        nodejs 'node-20'
    }
    stages {
        stage('Clone Project') {
            steps {
                git 'https://github.com/ElQuenta/isc-system-web.git'
            }
        }
        stage('Install Dependencies') {
            steps {
                sh 'npm install'
            }
        }
        stage('cypress') {
            steps {
                sh 'npx cypress run --browser chrome --spec "cypress/e2e/loginPaul.cy.ts"'
            }
        }
    }
}